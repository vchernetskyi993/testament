ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [com.example.ApplicationKt.module]
  }
}

credentials {
  user = "admin"
  user = ${?PARTICIPANT_USER}
  password = "admin"
  password = ${?PARTICIPANT_PASSWORD}
}
